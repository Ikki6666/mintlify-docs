---
api: "POST http://api.dev.origin-power.com/api/v3/videos"
title: "创建视频任务"
description: "提交文生视频或图生视频任务，异步生成 MP4 视频。"
---

**POST** `/api/v3/videos`

提交视频生成任务，立即返回任务 ID。视频生成为异步任务制：提交后用任务 ID 到[查询视频任务](/api-reference/video-task-query)轮询状态，完成后获取下载链接。

当前模型为 `minimax-h3`，输出 MP4（H.264 + AAC 音轨）、768P 档、4~15 秒、24fps。

<RequestExample>

```bash cURL
curl http://api.dev.origin-power.com/api/v3/videos \
  -H "Authorization: Bearer $ORIGIN_POWER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "minimax-h3",
    "prompt": "一只橘猫在雪地里打滚，电影感",
    "duration_seconds": 6,
    "resolution": "768P",
    "ratio": "16:9",
    "num_inference_steps": 50
  }'
```

</RequestExample>

<ResponseExample>

```json Response
{ "data": { "id": "video_5f3ae21", "status": "queued" } }
```

</ResponseExample>

## 请求体

<ParamField body="model" type="string" required>
  模型名，当前为 `minimax-h3`。
</ParamField>

<ParamField body="prompt" type="string" required>
  提示词，≤ 2000 字符，中英文均可。推荐“场景 + 主体 + 动作 + 运镜 + 风格”五要素写法。
</ParamField>

<ParamField body="duration_seconds" type="integer" default="6">
  视频时长（秒），4 ~ 15 的整数。帧率固定 24fps。
</ParamField>

<ParamField body="resolution" type="string" default="768P">
  分辨率档位，当前仅 `768P`（历史值 `720p` 自动归并）。
</ParamField>

<ParamField body="ratio" type="string" default="16:9">
  画面比例，可选 `16:9` / `9:16` / `1:1` / `4:3` / `3:4` / `21:9`。文生视频不支持 `adaptive`。
</ParamField>

<ParamField body="num_inference_steps" type="integer" default="50">
  去噪步数（画质档），4 ~ 50 的整数。默认 50 为满画质；速度优先可降至 8~12，画质下降，适合草稿。
</ParamField>

<ParamField body="input_reference" type="string">
  图生视频必填。首帧图片，推荐 Base64 Data URI（`data:image/png;base64,...`），也接受公网可访问的图片 URL。要求 PNG / JPEG / WebP，单张 ≤ 5 MB；建议 `ratio` 与图片宽高比一致。
</ParamField>

## 画面比例与输出尺寸

| ratio | 输出尺寸 |
| :--- | :--- |
| 16:9 | 1344×768 |
| 9:16 | 768×1344 |
| 1:1 | 768×768 |
| 4:3 | 1024×768 |
| 3:4 | 768×1024 |
| 21:9 | 1792×768 |

## 请求示例

文生视频：

```json
{
  "model": "minimax-h3",
  "prompt": "一只橘猫在雪地里打滚，电影感",
  "duration_seconds": 6,
  "resolution": "768P",
  "ratio": "16:9",
  "num_inference_steps": 50
}
```

图生视频（首帧）：

```json
{
  "model": "minimax-h3",
  "prompt": "画面动起来：战旗猎猎飞扬，烽烟翻滚，镜头缓缓推近",
  "duration_seconds": 4,
  "ratio": "16:9",
  "input_reference": "data:image/png;base64,..."
}
```

## 响应

HTTP 202，毫秒级返回。`data.id` 即任务 ID（task_id），后续所有操作使用它。

```json
{ "data": { "id": "video_5f3ae21", "status": "queued" } }
```

<Note title="提交前检查在途任务">
  单个 API Key 在途视频任务 ≤ 2。提交前先查询已有任务，或等任务完成后再提交。生成耗时参考：6 秒 × 50 步 ≈ 2 分钟。
</Note>
